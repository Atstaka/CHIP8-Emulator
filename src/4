#include <stdint.h>
#include <stdio.h>
#include "CHIP8.h"

void initialize(chip8* chip){
	chip->PC = PC_START;
	chip->I = 0;
	chip->stackPointer = 0;
}

void loadROM(chip8* chip, const char* file){
	FILE *file = fget(file);
}

void cycle(chip8* chip){
	
}


